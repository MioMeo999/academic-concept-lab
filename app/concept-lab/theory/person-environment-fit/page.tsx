import type { Metadata } from "next";
import { ContentPage } from "../../ConceptLab";

export const metadata: Metadata = { title: "Person–Environment Fit · Concept Lab", description: "Explore Person–Environment Fit through three visual directions and three reading depths." };
export default function TheoryPage() { return <ContentPage kind="theory"/>; }
